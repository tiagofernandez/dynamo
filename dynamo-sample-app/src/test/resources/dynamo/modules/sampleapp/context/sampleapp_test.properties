# Entity Manager
entityManager.dialect = org.hibernate.dialect.HSQLDialect
entityManager.hbm2ddl = create-drop
entityManager.showSql = false

# DataSource
dataSource.driverClassName = org.hsqldb.jdbcDriver
dataSource.url = jdbc:hsqldb:mem:sampleapp
dataSource.username = sa
dataSource.password = 

# Web server settings
webServer.host = localhost
webServer.port = 8090
webServer.context = sampleapp