<!DOCTYPE suite SYSTEM "http://testng.org/testng-1.0.dtd">
<suite name="${artifactId}">
	<test name="all" annotations="JDK">
		<packages>
			<package name="${package}.${artifactId}.*" />
		</packages>
	</test>
</suite>